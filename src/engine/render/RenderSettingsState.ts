/** Shared render settings applied by RenderSettingsSystem. */
export class RenderSettingsState {
  wireframe = false
  /** When true, also apply wireframe to static (pre-activation) meshes for preview. */
  applyToStatic = false
}
